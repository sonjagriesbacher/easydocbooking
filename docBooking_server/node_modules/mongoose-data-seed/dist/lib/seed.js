'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _lodash = require('lodash');

var _lodash2 = _interopRequireDefault(_lodash);

var _config = require('./config');

var _config2 = _interopRequireDefault(_config);

var _utils = require('./utils');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var seed = function seed(selectedSeeders) {
  if (!Array.isArray(selectedSeeders)) {
    selectedSeeders = [];
  }

  (0, _utils.mustContainUserConfig)();

  var seedersList = _config2.default.userConfig.seedersList;


  var seeders = selectedSeeders.length > 0 ? (0, _utils.getObjectWithSelectedKeys)(seedersList, selectedSeeders.map(function (name) {
    return _lodash2.default.upperFirst(_lodash2.default.camelCase(name));
  })) : seedersList;

  return (0, _utils.runSeeders)(seeders);
};

exports.default = seed;